components:
  schemas:
    Store:
      required:
        - id
        - name
        - storeType
        - address
        - url
        - placeId
      properties:
        id:
          type: string
        name:
          type: string
        storeType:
          type: string
        address:
          type: string
        url:
          type: string
        placeId:
          type: string
    CreateStore:
      type: object
      required:
        - name
        - storeType
        - address
        - url
        - placeId
      properties:
        name:
          type: string
        storeType:
          type: string
        address:
          type: string
        url:
          type: string
        placeId:
          type: string

components:
  schemas:
    PlaceType:
      type: object
      description: place type
      required:
        - id
        - label
      properties:
        id:
          type: string
          description: place id
        key:
          type: string
          description: value
        name:
          type: string
          description: display name

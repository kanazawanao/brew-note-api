components:
  schemas:
    RecipeStep:
      required:
        - id
        - stepNumber
        - seconds
        - amountWater
        - memo
      properties:
        id:
          type: integer
        stepNumber:
          type: integer
        seconds:
          type: integer
        amountWater:
          type: integer
        memo:
          type: string
    CreateRecipeStep:
      required:
        - stepNumber
        - seconds
        - amountWater
        - memo
      properties:
        stepNumber:
          type: integer
        seconds:
          type: integer
        amountWater:
          type: integer
        memo:
          type: string

components:
  schemas:
    RoastLevel:
      required:
        - id
        - level
        - name
      properties:
        id:
          type: integer
        level:
          type: integer
        name:
          type: string

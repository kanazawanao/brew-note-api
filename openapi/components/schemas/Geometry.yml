components:
  schemas:
    Geometry:
      properties:
        location:
          $ref: ./Location.yml#/components/schemas/Location
        viewport:
          $ref: ./Viewport.yml#/components/schemas/Viewport